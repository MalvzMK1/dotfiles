alias ll="ls -lah"
alias sts="npm run start:dev:all"
alias tpchk="npx tsc --noEmit"
alias reset-database="npm run migrate:reset -- --force && npm run db:seed:dev"
alias nr="npm run"
alias code="codium"
alias ws="windsurf"
alias shutdown="sudo shutdown now"
alias reboot="sudo reboot now"
alias pnr="pnpm run"
alias py="python3"
alias commit="better-commits"

# docker aliases
alias dcu="docker compose up"
alias dcd="docker compose down"
alias dl="docker logs"
alias dps="docker ps"

# utils
alias myip="curl checkip.amazonaws.com"
alias make-repo-personal="sh ~/scripts/use-personal-git.sh"

# TMUX
alias tmn="tmux new -s"
alias tma="tmux a"
alias tmas="tmux a -t"
alias tmls="tmux ls"
